@model IEnumerable<SolucionCacao.Models.Ficha>

@{
    ViewData["Title"] = "Index";
    ViewData["IdFicha"] = "";
}

<h1>Fichas</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Crear Ficha</a>
</p>
<table class="table table-success table-striped table-bordered">
    <thead>
        <tr>
            <th>
                Nombre
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                Técnico
            </th>
            <th>
                Lugar
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>

@foreach (var item in Model) {

        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreFicha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTecnicoNavigation.Nombres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdZonaNavigation.Lugar)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">Editar</a> 
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Detalles</a> 
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger"> Eliminar</a> 
                <a asp-controller="lineaFichas" asp-action="Index" asp-route-id="@item.Id" class="btn btn-light">Ver</a>
           </td>
        </tr>
}
    </tbody>
</table>
